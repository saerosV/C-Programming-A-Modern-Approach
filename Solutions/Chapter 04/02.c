#include <stdio.h>

// Reverts a three digit number entered by the user.

int main (void)
{
	int i;
	
	printf("Enter a three digit number: ");
	scanf("%d", &i);
	
    printf("The reversal is: %d%d%d\n", n % 10, n / 10 % 10, n / 100);
	
	return 0;
}
